<UserControl x:Class="IFlexV1_Sample.SearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IFlexV1_Sample"
             xmlns:input="clr-namespace:System.Windows.Controls"
             mc:Ignorable="d" 
             d:DesignWidth="250" d:DesignHeight="30">
    <Grid x:Name="LayoutRoot">
        <Grid.Resources>
            <local:HighlightTextInstantSearchConverter x:Key="HighlightTextInstantSearchConverter"/>
        </Grid.Resources>
        <input:AutoCompleteBox x:Name="acbPersonnel" SearchKind="Personnel" FilterMode="Contains" IsTextCompletionEnabled="False" VerticalAlignment="Center" Canvas.ZIndex="1">
            <input:AutoCompleteBox.ItemTemplate>
                <DataTemplate>
                    <local:HighlightingTextBlock HighlightText="{Binding SearchText, ElementName=acbPersonnel, Converter={StaticResource HighlightTextInstantSearchConverter}}" Text="{Binding}" HighlightBrush="#FF44A0FF"/>
                </DataTemplate>
            </input:AutoCompleteBox.ItemTemplate>
        </input:AutoCompleteBox>
    </Grid>
</UserControl>
